pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html public/
    - cp style.css public/
    - cp app.js public/
  artifacts:
    paths:
      - public
  only:
    - main  